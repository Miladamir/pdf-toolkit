name = "pdf-toolkit"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Bind an R2 Bucket to handle large files
[[r2_buckets]]
binding = "BUCKET" 
bucket_name = "pdf-toolkit-bucket" # You must create this bucket in your Cloudflare Dashboard